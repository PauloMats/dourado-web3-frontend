import styled from 'styled-components';

export const Container = styled.footer`
  a {
    transition: color 0.2s;
    &:hover {
      color: #FFD700;
    }
  }

  svg {
    stroke-width: 1.5;
  }
`;
